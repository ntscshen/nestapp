/**
 * 预定义运行环境
 * production 用于生成环境
 */
export enum EnvironmentType {
    DEVELOPMENT = 'development',
    DEV = 'dev',
    PRODUCTION = 'production',
    PROD = 'prod',
    TEST = 'test',
    PREVIEW = 'preview',
}
