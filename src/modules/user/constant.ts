export enum UserValidateGroup {
    CREATE = 'user-create',
    UPDATE = 'user-update',
    REGISTER = 'user-register',
}

export enum UserOrderType {
    CREATED = 'createdAt',
    UPDATE = 'updatedAt',
}
